191127
전역 객체
모든 객체는 이 전역객체의 프로퍼티(객체 내의 변수)이다.
*Node.js에는 전역객체의 메소드가 존재하지 않는다.
------------
this
this는 함수 내에서 함수 호출의 맥락(context)를 의미함.
즉, 상황에 따라 달라질 수 있음
function func(){
    if(window === this){
        document.write("window === this");
    }
}
func(); 
//결과 : window == this
